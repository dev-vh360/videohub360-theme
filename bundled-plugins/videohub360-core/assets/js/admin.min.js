(function($) {
'use strict';
function initShortcodesPage() {
$('.vh360-copy-btn').on('click', function(e) {
e.preventDefault();
var button = $(this);
var shortcode = button.data('shortcode');
function showSuccess() {
var originalHtml = button.html();
button.html('<span class="dashicons dashicons-yes"></span> ' + vh360Admin.copiedText);
button.addClass('vh360-copied');
setTimeout(function() {
button.html(originalHtml);
button.removeClass('vh360-copied');
}, 2000);
}
if (navigator.clipboard && navigator.clipboard.writeText) {
navigator.clipboard.writeText(shortcode).then(function() {
showSuccess();
}).catch(function(err) {
console.error('Clipboard API failed, trying fallback:', err);
copyFallback();
});
} else {
copyFallback();
}
function copyFallback() {
var tempTextarea = $('<textarea>');
tempTextarea.val(shortcode);
tempTextarea.css({
position: 'absolute',
left: '-9999px'
});
$('body').append(tempTextarea);
tempTextarea.select();
try {
var successful = document.execCommand('copy');
if (successful) {
showSuccess();
} else {
console.error('Copy command was unsuccessful');
}
} catch(err) {
console.error('Failed to copy:', err);
}
tempTextarea.remove();
}
});
}
$(document).ready(function() {
if ($('.vh360-shortcodes-page').length) {
initShortcodesPage();
}
});
})(jQuery);
